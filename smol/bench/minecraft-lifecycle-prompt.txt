Build a complete, playable Minecraft-inspired voxel sandbox called Block Meadow in this empty workspace. Use JavaScript modules, Three.js and Vite with project-local npm dependencies. Create original visuals with geometry and colors; no external artwork is needed. Keep modules reasonably small so they can be edited independently.

Required behavior:
- A seeded, deterministic terrain at least 48 by 48 blocks with hills, grass/dirt/stone layers, trees and a small lake. Render efficiently with instancing or exposed-face meshes.
- First-person mouse look with pointer lock, WASD movement, gravity, grounded jumping, and solid collision on all axes. Spawn safely above the terrain. Clamp large frame deltas so resuming a background tab does not launch or bury the player.
- A visible target outline and crosshair. Left click removes the targeted block within reach; right click places a block on the adjacent face. Never place inside the player's body or outside the world. Number keys 1-5 select grass, dirt, stone, wood and leaves; show the selected block in a hotbar.
- An attractive start/pause screen, concise controls, a terrain/sky color palette, ambient and directional lighting, and a live FPS display. Escape unlocks the pointer and pauses movement; resuming works repeatedly.
- Keep the crosshair, FPS and five-slot hotbar visible during play, including after reload and New World. Use visible HTML elements with aria-label="Crosshair", "FPS" and "Hotbar"; mark only the selected hotbar button aria-pressed="true" and update its visible highlight when number keys change selection.
- Save terrain edits, selected block and player position in localStorage. Reload restores them. Include visible Save and New World controls; New World asks for confirmation and creates a clean world. Invalid saved data must not crash startup.
- Separate world and collision logic from rendering and add meaningful automated tests with Node's built-in test runner. Cover deterministic terrain, block bounds, edit persistence, and collision/jump rules. npm test and npm run build must pass.
- For browser verification, expose window.voxelDebug with snapshot() returning { player: { x, y, z }, selected: blockType, targeted: { x, y, z } or null, editCount: number }; getBlock(x,y,z) returning the actual block type (0 for air); and teleport(x,y,z). Use these exact field names. This must inspect the actual live game state.

Create the application yourself using tools. Plan the work, implement it, run and fix the tests and production build, and write a README with controls and exact start commands. Do not claim a browser check unless you actually performed one. Finish with a concise result and any known limitations.
