# AGENTS.md 템플릿

**사용법:** 프로젝트 루트에 `AGENTS.md`로 복사하고 `{{...}}` 부분을 채우세요.
프로젝트마다 다른 건 지우고, 남는 항목만 두세요. 규칙이 많을수록 모델이 무시하므로 **10~20줄이 목표**입니다.

---

# AGENTS.md — {{프로젝트 이름}}

{{한 줄 설명: 무엇을 하는 프로젝트인지}}

## 🚨 명령어

```bash
{{설치}}     # {{예: npm install}}
{{개발}}     # {{예: npm run dev}}
{{빌드}}     # {{예: npm run build}}
{{테스트}}   # {{예: npm test}} ← 이게 통과해야 작업을 끝낸 것으로 본다
{{린트/포맷 }} # {{없으면 이 줄 삭제}}
```

- {{예: 테스트가 빌드를 포함하지 않으면, 반드시 빌드 후 테스트를 돌릴 것}}

## 🚨 절대 규칙

1. **생성물은 직접 수정하지 않는다.** {{예: dist/, build/, *.min.js}} — 원본을 고치고 다시 빌드.
2. **비밀값을 커밋하지 않는다.** {{.env, 토큰, 키}} 는 .gitignore에 두고 코드·로그·테스트에 하드코딩하지 않는다.
3. **확인 없이 API·엔드포인트·옵션을 만들지 않는다.** 소스·문서를 먼저 읽고, 모르면 사용자에게 물어본다.
4. **기존 스타일을 따른다.** {{예: 들여쓰기, 따옴표, 변수 선언 규칙, 포맷터 설정}}

## 🧪 테스트 규칙

- 버그를 고치면 **그 버그를 재현하는 테스트를 먼저 추가**한다.
- 테스트는 {{실행 명령}}로 확인한다. 통과 전에는 작업을 끝내지 않는다.
- 회귀가 의심되면 {{회귀 확인 방법}}.

## 📁 구조

```
{{최상위 디렉터리와 각 역할 한 줄씩}}
```

## 📄 기타

- {{문서 위치와 언어, 코드 주석 언어}}
- {{커밋/PR 규약}}
